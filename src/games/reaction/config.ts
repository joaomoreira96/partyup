import type { GameConfigSpec } from "@/lib/partyup-sdk/types";

export const reactionConfig: GameConfigSpec = {
  name: "Reação Rápida",
  slug: "reacao-rapida",
  version: "1.0.0",
  supportsMobile: true,
  supportsTablet: true,
  supportsDesktop: true,
  minPlayers: 1,
  maxPlayers: 1,
};
